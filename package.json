{
  "name": "eureka",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "oxlint": "latest",
    "oxfmt": "latest",
    "typescript": "^5.7.3"
  }
}
